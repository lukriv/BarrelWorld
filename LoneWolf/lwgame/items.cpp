#include "items.h"

ItemAndWeaponManager::AddBagItem(EBagItems type, const wxString& title, const wxString& desc)
{
}

ItemAndWeaponManager::AddSpecialItem(ESpecialItems type, const wxString& title, const wxString& desc)
{
}

ItemAndWeaponManager::AddWeapon(EWeapons type, const wxString& title, const wxString& desc)
{
}
